<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Boat Management Categories -->
        <record id="module_category_boat_management" model="ir.module.category">
            <field name="name">Boat Management</field>
            <field name="description">Manage boat rental operations</field>
            <field name="sequence">10</field>
        </record>

        <!-- Boat User Group -->
        <record id="group_boat_user" model="res.groups">
            <field name="name">User</field>
            <field name="category_id" ref="module_category_boat_management"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <!-- Boat Manager Group -->
        <record id="group_boat_manager" model="res.groups">
            <field name="name">Manager</field>
            <field name="category_id" ref="module_category_boat_management"/>
            <field name="implied_ids" eval="[(4, ref('group_boat_user'))]"/>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>

        <!-- Record Rules -->
        <!-- Boat User: Can see all boats -->
        <record id="boat_boat_user_rule" model="ir.rule">
            <field name="name">Boat User: See All Boats</field>
            <field name="model_id" ref="model_boat_boat"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_boat_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Boat Manager: Full access -->
        <record id="boat_boat_manager_rule" model="ir.rule">
            <field name="name">Boat Manager: Full Access</field>
            <field name="model_id" ref="model_boat_boat"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_boat_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>