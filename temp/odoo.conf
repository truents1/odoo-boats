[options]
; --- File System and Path Configuration ---
addons_path = /usr/lib/python3/dist-packages/odoo/addons,/mnt/extra-addons
data_dir = /var/lib/odoo

; --- Database Connection Configuration ---
; Hostname MUST match the database SERVICE name
;db_host = odoo-boats-db
db_port = 5432
; User is typically not sensitive, but we'll use a standard value
db_user = odoo

; 🔑 SOLUTION: Use shell expansion to pull the password from the environment.
; The Odoo container will substitute this value at startup using the variable
; defined in the 'environment' section of docker-compose.yml (which pulls from .env).
db_password = Semarang@2015

; --- Server Configuration ---
; db_name = odoo-boats-db
